
# Entity Framework 
dotnet new console -o EFGetStarted

cd EFGetStarted

dotnet add package Microsoft.EntityFrameworkCore.SqlServer 

dotnet add package Microsoft.EntityFrameworkCore.Design

dotnet ef dbcontext scaffold "Server=localhost;Database=tuber_database;User Id=sa;Password=myPassw0rd;" Microsoft.EntityFrameworkCore.SqlServer



sudo docker ps
sudo docker ps -a
sudo docker-compose -f mssql.yml up -d
sudo docker-compose -f mssql.yml down
sudo docker system prune

sudo mssql -s localhost -u sa -p myPassw0rd

//system databases (master, model, msdb, tempdb)

.databases
.tables

USE master;
CREATE DATABASE tuber_database;
DROP DATABASE login;

.run dbcreate.sql
.run dbload.sql
.run dbdrop.sql

SELECT * FROM USERS;

